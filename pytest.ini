[pytest]
minversion = 6.0
testpaths =
    tests
# tests/local needs local model weights and is run explicitly: pytest tests/local
norecursedirs = local
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
		ignore::FutureWarning
pythonpath = .
